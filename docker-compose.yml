services:
  backend:
    build: ./backend
    container_name: vacaciones_backend
    env_file:
      - .env
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: vacaciones_frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend